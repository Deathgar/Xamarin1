<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:customElements="clr-namespace:Test3.Custom_Elements;assembly=Test3"
             x:Class="Test3.ChangePokemonPage">
    <ContentPage.Resources>
        <ResourceDictionary>
            <Style x:Key="bottomButtons" TargetType="Button">
                <Setter Property="HorizontalOptions" Value="Center"/>
                <Setter Property="VerticalOptions" Value="End"/>
                <Setter Property="Margin" Value="0,0,0,8"/>
                <Setter Property="HeightRequest" Value="45"/>
                <Setter Property="WidthRequest" Value="100"/>
                <Setter Property="FontSize" Value="10"/>
            </Style>
        </ResourceDictionary>
    </ContentPage.Resources>
    <ContentPage.Content>
        <StackLayout Padding="10,10,0,0">
            <ScrollView>
                <StackLayout WidthRequest="20">
                    <!--<Label Text="PokemonName:" FontSize="Medium" FontAttributes="Bold"></Label>
                    <Label x:Name="PokemonName" FontSize="Small" Margin="10,0,0,0" FontAttributes="Bold"></Label>-->
                    <StackLayout Orientation="Horizontal">
                        <Label Text="Name:" FontSize="Medium" Margin="0,6,0,0"></Label>

                        <RelativeLayout>
                            <Entry x:Name="Name" FontSize="12" WidthRequest="160" Margin="11,11,0,0"></Entry>
                        </RelativeLayout>
                    </StackLayout>

                    <StackLayout Orientation="Horizontal" Margin="0,8,0,0">
                        <Label Text="Height:" FontSize="Medium"></Label>
                        <RelativeLayout>
                            <customElements:SquadEntry WidthRequest="160" x:Name="Height" FontSize="15" Margin="6,4,0,0">
                                <customElements:SquadEntry.Behaviors>
                                    <customElements:ValidTextNumbBehavior/>
                                </customElements:SquadEntry.Behaviors>
                            </customElements:SquadEntry>
                        </RelativeLayout>
                    </StackLayout>

                    <StackLayout Orientation="Horizontal" Margin="0,8,0,0">
                        <Label Text="Weight:" FontSize="Medium"></Label>
                        <RelativeLayout>
                            <customElements:SquadEntry WidthRequest="160" x:Name="Weight" FontSize="15" Margin="4,4,0,0">
                                <customElements:SquadEntry.Behaviors>
                                    <customElements:ValidTextNumbBehavior/>
                                </customElements:SquadEntry.Behaviors>
                            </customElements:SquadEntry>
                        </RelativeLayout>
                    </StackLayout>
                </StackLayout>
            </ScrollView>
            <StackLayout Orientation="Horizontal" Padding="15">
                <Button Text="Change" Style="{StaticResource bottomButtons}" Command="{Binding ChangeCommand}"></Button>
                <Button Text="Cancel" Style="{StaticResource bottomButtons}" Clicked="Cancel"></Button>
            </StackLayout>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>